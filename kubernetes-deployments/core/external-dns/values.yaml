external-dns:
  enabled: true
  fullnameOverride: external-dns
  provider: pihole
  policy: upsert-only
  registry: noop
  env:
  - name: EXTERNAL_DNS_PIHOLE_PASSWORD
    valueFrom:
      secretKeyRef:
        name: pihole-pass
        key: password
  extraArgs:
  - --pihole-server=http://192.168.1.77
  resources:
    limits:
      cpu: 50m
      memory: 96Mi
    requests:
      cpu: 10m
      memory: 32Mi
